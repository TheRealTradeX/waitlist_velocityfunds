## Copy to `.env.local` for local development only.
## Do not commit `.env.local` (it is gitignored).

# Protects GET /api/waitlist-stats
WAITLIST_STATS_TOKEN=

# Optional: override the default table name (`waitlist_signups`)
# WAITLIST_TABLE=waitlist_signups
